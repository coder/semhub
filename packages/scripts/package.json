{
  "name": "@semhub/scripts",
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@semhub/core": "*"
  },
  "scripts": {
    "deploy:cf": "sst shell -- bun src/wrangler.ts deploy",
    "deploy:cf:prod": "sst shell -- bun src/wrangler.ts deploy --env prod",
    "typecheck": "tsc --noEmit",
    "shell": "sst shell -- bun",
    "shell:prod": "sst shell --stage prod -- bun"
  }
}
