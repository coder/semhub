{
  "name": "@semhub/scripts",
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@semhub/core": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate:go": "bun src/generate-go.ts",
    "shell": "sst shell -- bun",
    "shell:uat": "sst shell --stage uat -- bun",
    "shell:prod": "sst shell --stage prod -- bun"
  }
}
