{
  "name": "@semhub/workers",
  "version": "0.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --build",
    "auth:generate": "sst shell -- bunx @better-auth/cli generate --config ./src/auth.ts --output ../core/src/db/schema/entities/auth-schema.ts"
  },
  "dependencies": {
    "@semhub/core": "workspace:*",
    "@semhub/wrangler": "workspace:*",
    "@hono/zod-validator": "^0.4.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0"
  }
}
