{
  "name": "@semhub/wrangler",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc",
    "build": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "dev": "bun scripts/worker.ts dev",
    "deploy:dev": "bun scripts/worker.ts deploy",
    "delete": "bun scripts/worker.ts delete",
    "deploy:all": "bun run cf:deploy rate-limiter && bun run cf:deploy workflow"
  },
  "dependencies": {
    "@semhub/core": "*",
    "p-map": "^7.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "wrangler": "^3.91.0",
    "vitest": "^2.1.6"
  }
}
